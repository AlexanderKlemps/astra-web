upstream astraweb {
    server generator:8000;
}

server{
   listen 80;
   listen  [::]:80;
   server_name  localhost;

   include /etc/nginx/mime.types;

   location /astra/ {
      proxy_pass http://astraweb/;
   }
}